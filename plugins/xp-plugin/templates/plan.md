# Execution Plan

## Milestone 1 — <name>   [in-progress]
Goal: <one sentence>
Done when: <an EXECUTABLE check — a command that exits 0 when the milestone is real>

### Sprint 1

#### story-000 — <replace this example card>   [planned]
Context: <what this story owns, and only this story>
Files: <the files it will touch>
AC:
- Given <precondition>, When <action>, Then <observable result>
Verify: EDIT-ME  # the command(s) that execute the ACs at the surface, exit 0 = done
Executor: (default)
